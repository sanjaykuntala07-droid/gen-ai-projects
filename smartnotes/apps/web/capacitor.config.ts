import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.sanjaykuntala.smartnotes',
  appName: 'SmartNotes',
  webDir: 'dist'
};

export default config;
